{
  "name": "versowaves",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "allure:copy-history": "if exist allure-report\\history xcopy /E /I /Y allure-report\\history allure-results\\history",
    "allure:report": "npm run allure:copy-history && allure generate allure-results -o allure-report && allure open",
    "test:report": "npm run test && npm run allure:report",
    "mcp:start": "node mcp/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.14.1",
    "allure-commandline": "^2.34.0",
    "allure-playwright": "^3.2.1",
    "@modelcontextprotocol/sdk": "^0.6.0"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
