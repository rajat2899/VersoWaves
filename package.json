{
  "name": "versowaves",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "allure:clean": "rd /s /q allure-results && rd /s /q allure-report",
    "allure:report": "npm run allure:clean && allure generate allure-results --clean && allure open",
    "test:report": "npm run test && npm run allure:report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@types/node": "^22.14.1",
    "allure-commandline": "^2.34.0",
    "allure-playwright": "^3.2.1"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}
